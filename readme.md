This is the automated messsage software using selenium and python . just clone the repo and and install all the dependencies
run command python <filename>
you have to login whatsapp into browser , as it opens automatically. 

for now send photos videos is not working , only send message is working
